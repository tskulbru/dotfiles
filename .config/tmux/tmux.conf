source-file ~/.config/tmux/tmux.reset.conf
set-option -g default-terminal 'screen-256color'
set-option -g terminal-overrides ',xterm-256color:RGB'

set -g prefix ^a
set -g base-index 1              # start indexing windows at 1 instead of 0
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g escape-time 0             # zero-out escape time delay
set -g history-limit 1000000     # increase history size (from 2,000)
set -g renumber-windows on       # renumber all windows when any window is closed
set -g set-clipboard on          # use system clipboard
set -g status-position top       # macOS / darwin style
set -g default-terminal "${TERM}"
setw -g mode-keys vi
set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'
set -g mouse on

# TPM - Tmux Plugin Manager
set -g @plugin 'tmux-plugins/tpm'
# Basic tmux settings everyone can agree on
set -g @plugin 'tmux-plugins/tmux-sensible'
# Enhanced clipboard integration
set -g @plugin 'tmux-plugins/tmux-yank'
# https://github.com/tmux-plugins/tmux-resurrect
set -g @plugin 'tmux-plugins/tmux-resurrect'
# Resume Claude Code (and other AI assistant) sessions on restore.
# MUST be declared after tmux-resurrect — it hooks resurrect's save/restore.
set -g @plugin 'timvw/tmux-assistant-resurrect'
# Automatic save (every @continuum-save-interval min) + restore on server start.
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @resurrect-strategy-nvim 'session'
set -g @continuum-save-interval '5'
set -g @continuum-restore 'on'

# Status bar configuration - universal settings
set -g status-left-length 100
set -g status-right-length 0
set -g status-right ''
set -g window-status-separator ''

# Custom pane resizing
bind-key M-Up resize-pane -U 300
bind-key M-Down resize-pane -D 300
bind-key M-Left resize-pane -L 300
bind-key M-Right resize-pane -R 300

# theme-specific configuration
source ~/.config/tmux/tmux.theme.conf

source ~/.config/tmux/tmux.tskulbru.conf
# Initialize TPM (Tmux Plugin Manager)
run '~/.tmux/plugins/tpm/tpm'
